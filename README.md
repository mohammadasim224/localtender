# LocalTender

Bradford council jobs for local firms. Match by trade, one-page brief, bid help. Yearly fee.

The site runs without backend keys. Sign-up is stored in the browser for demo.

```bash
npm install
npm run dev
```

Connect later: Convex, Clerk, Stripe, PostHog, Resend.
